{
  "name": "instagram-followers",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch run index.ts",
    "build": "bun run build:clean && bun run build:bun",
    "build:bun": "bun build --target=bun --minify index.ts --outfile=./bundle.js",
    "build:clean": "rm -rf bundle.js",
    "start": "bun run bundle.js"
  },
  "devDependencies": {
    "bun-types": "^1.0.1",
    "prettier": "^2.8.3",
    "typescript": "^4.9.4"
  }
}

{
  "name": "instagram-followers",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch run index.ts",
    "build": "bun run build:clean && bun run build:bun",
    "build:bun": "bun build index.ts --target=bun --minify --outfile=./bundle.js",
    "build:clean": "rm -rf bundle.js",
    "start": "bun run bundle.js"
  },
  "devDependencies": {
    "bun-types": "^1.0.15",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2"
  }
}
